apiVersion: apps/v1
kind: Deployment
metadata:
  name: video-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: video-api
  template:
    metadata:
      labels:
        app: video-api
    spec:
      containers:
      - name: video-api
        image: YOUR_DOCKERHUB_USERNAME/video-api:latest  # <--- Updated this
        imagePullPolicy: Always                          # <--- Updated this
        ports:
        - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: video-api-service
  labels:                  #new added
    app: video-api         #new added
spec:
  type: NodePort
  selector:
    app: video-api
  ports:
  - name: http-web         #new added
    port: 5000
    targetPort: 5000
    nodePort: 30000
